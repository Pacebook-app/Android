<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pacebook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!--bottom Navigation transparent 뒤에 동그라미를 없앤다-->
    <style name="BottomNav_NoIndicator" parent="">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Theme.Pacebook" parent="Base.Theme.Pacebook" />
    <style name="MyRoundedDialogTheme" parent="ThemeOverlay.Material3.Dialog">
        <item name="dialogCornerRadius">16dp</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>
</resources>